kind = "mobile"
previewPath = "/"
title = "Mahjong Den"
version = "1.0.0"
id = "artifacts/mahjong-den"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 25351

[services.development]
run = "pnpm --filter @workspace/mahjong-den run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mahjong-den", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/mahjong-den", "run", "serve" ]

[services.env]
PORT = "25351"
BASE_PATH = "/"
